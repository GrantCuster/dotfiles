## Base16 Gruvbox dark, medium
# Author: Dawid Kurek (dawikur@gmail.com), morhetz (https://github.com/morhetz/gruvbox)
#
# You can use these variables anywhere in the i3 configuration file.

set $base00 #1d2021
set $base01 #3c3836
set $base02 #504945
set $base03 #665c54
set $base04 #bdae93
set $base05 #d5c4a1
set $base06 #ebdbb2
set $base07 #fbf1c7
set $base08 #fb4934
set $base09 #fe8019
set $base0A #fabd2f
set $base0B #b8bb26
set $base0C #8ec07c
set $base0D #83a598
set $base0E #d3869b
set $base0F #d65d0e

# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:IBM Plex Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec st
bindsym $mod+shift+Return exec st -d "$(readlink -e /proc/"$(pgrep -oP "$(xdo pid)")"/cwd)"

# kill focused window
bindsym $mod+q kill

# open firefox
# bindsym $mod+g exec firefox

# open firefox dev
bindsym $mod+f exec fd

# open config
bindsym $mod+c exec st -e zsh -i -c 'vim ~/.config'

# stop ffmpeg
bindsym $mod+x exec killall --user $USER --ignore-case --signal INT ffmpeg

set $bg #282828
set $fg #ebdbb2
set $red #cc241d
set $green #98971a
set $yellow #d79921
set $blue #458588
set $purple #b16286
set $aqua #689d68
set $gray #a89984
set $black #1d2021


# start dmenu (a program launcher)
bindsym $mod+space exec "dmenu_run -fn 'JetBrains Mono:pixelsize=14:style=bold' -nb '#1d2021' -sf '#282828' -sb '#ebdbb2' -nf '#ebdbb2' -h 22"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# change active workspace
bindsym $mod+Shift+Tab workspace prev
bindsym $mod+Left workspace prev
bindsym $mod+Tab workspace next
bindsym $mod+Right workspace next

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
# bindsym $mod+s split h

# split in vertical orientation
# bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+t fullscreen toggle

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+d focus mode_toggle


bindsym $mod+w exec st -e zsh -i -c "vim -c VimwikiIndex ."


# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# keyboard backlight
bindsym Shift+F10 exec $HOME/Scripts/gruvkey_off
bindsym Shift+F11 exec $HOME/Scripts/gruvkey_on

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% 
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle

# turn off mouse warping for mulltiple displalys
# mouse_warping none

# assign workspaces to screens
# workspace 1 output eDP-1
# workspace 2 output eDP-1
# workspace 3 output eDP-1
# workspace 4 output eDP-1
# workspace 5 output eDP-1

workspace 6 output DVI-I-2-2
workspace 7 output DVI-I-2-2
workspace 8 output DVI-I-2-2
workspace 9 output DVI-I-2-2
workspace 10 output eDP-1

workspace 1 output DVI-I-1-1
workspace 2 output DVI-I-1-1
workspace 3 output DVI-I-1-1
workspace 4 output DVI-I-1-1
workspace 5 output DP-1


# workspace 1 output eDP-1
# workspace 2 output eDP-1
# workspace 3 output eDP-1
# workspace 4 output eDP-1
# workspace 5 output eDP-1

# workspace 6 output DP-2
# workspace 7 output DP-2
# workspace 8 output DP-2
# workspace 9 output DP-2
# workspace 10 output DP-2

workspace 6 output eDP-1
workspace 7 output eDP-1
workspace 8 output eDP-1
workspace 9 output eDP-1
workspace 10 output eDP-1

# # workspace 6 output eDP-1
# # workspace 7 output eDP-1
# # workspace 8 output eDP-1
# # workspace 9 output eDP-1
# # workspace 10 output eDP-1

# workspace 1 output DP-2
# workspace 2 output DP-2
# workspace 3 output DP-2
# workspace 4 output DP-2
# workspace 5 output DP-2

# gaps inner 8

set $ws1 "1"
set $ws2 "2"
set $ws8 "8"

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 4 px
        bindsym j resize grow height 4 px
        bindsym k resize shrink height 4 px
        bindsym l resize grow width 4 px

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# bindsym $mod+shift+x exec $HOME/.config/i3lock/scripts/lock.sh

# Media player controls
# bindsym F4 exec dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.PlayPause
# bindsym F5 exec dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Previous
# bindsym F6 exec dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Next

# Pomodoro
# bindsym $mod+p exec "~/.config/scripts/pomodoro toggle"
# bindsym $mod+o exec "~/.config/scripts/pomodoro end"

# hide show polybar
bindsym $mod+b exec polybar-msg cmd toggle

# switch dark and light mode
bindsym $mod+o exec "~/.local/bin/toggle_light_mode.sh"

# switch between normal and presentation mode
bindsym $mod+i exec "~/.local/bin/toggle_i3_mode.sh"

# pomodoro
bindsym $mod+p exec "~/.config/scripts/pomodoro toggle"
bindsym $mod+n exec "~/.config/scripts/pomodoro end"

bindsym $mod+s layout stacking

# disable keyboard laptop 
# bindsym $mod+u exec $HOME/Scripts/toggle_touch

# exec_always feh --bg-scale $HOME/Pictures/grad.png
# exec_always feh --bg-scale /home/test/Pictures/adam-ferris1.jpg

# exec_always xset r rate 300 16
# exec_always setxkbmap -layout us -option ctrl:nocaps

# for_window [class=".*"] border normal 1
# for_window [class=".*"] border pixel 2
# for_window [class="Alacritty"] border pixel 1
# for_window [class="Alacritty" title=".*VIM"] border normal 1
# dev tools
# for_window [instance="chromium-browser"] border pixel 1
# for_window [class=".*"] border pixel 2
# hide_edge_borders smart

# set primary gruvbox colorscheme colors
set $bg #282828
set $fg #ebdbb2
set $red #cc241d
set $green #98971a
set $yellow #d79921
set $blue #458588
set $purple #b16286
set $aqua #689d68
set $gray #a89984
set $black #1d2021

# evangelion colors 
set $eblack #111111
set $efg #ffac0b
set $ered #fa3438

# graybit colors
set $gblack #111111
set $greg #191919
set $gfg #888888
set $fred #ff8e65

client.focused          $base00 $base00 $base00 $base00 $base00
client.focused_inactive $base00 $base00 $base00 $base00 $base00
client.unfocused        $base00 $base00 $base00 $base00 $base00
client.urgent           $base08 $base08 $base08 $base08 $base08

# bar set up is polybar

# exec alacritty
exec hsetroot -solid "#282828"
# exec_always --no-startup-id feh --bg-fill ~/Pictures/rooms.png
# exec_always --no-startup-id feh --bg-fill ~/s/eva/eva_background.png
exec_always --no-startup-id xrdb $HOME/.Xresources
exec --no-startup-id "~/.local/bin/multiple_polybar"
exec_always --no-startup-id xset r rate 220 24

# exec --no-startup-id st -c term1 -e zsh -c "neofetch && zsh"

exec_always --no-startup-id ~/script/autotiling.py

for_window [class=".*"] border pixel 1
